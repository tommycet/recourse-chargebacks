══════════════════════════════════════════════════════════════════════
  RECOURSE DEMO VIDEO v5 — NARRATION/SCENE SYNC MAP
  Video:  recourse_demo_v5.mp4 (120.167s)
  Audio:  narration_v5_full.wav (120.16s) — 10 Groq Orpheus TTS clips
  Gap:    0.007s (negligible)
══════════════════════════════════════════════════════════════════════

SCENE TIMELINE
──────────────────────────────────────────────────────────────────────

 #  NAME           START     END      DUR      WHAT SHOULD BE ON SCREEN
─── ────────────── ────────  ────────  ──────── ─────────────────────────────────────────
 1  hero            0.00s    13.76s   13.76s   Landing page hero section with "Recourse"
                                                headline + x402 badges
 2  primitives     13.76s    27.60s   13.84s   "What it does" section — 3 cards:
                                                Escrow / AI Arbiter / KeeperHub
 3  how            27.60s    38.88s   11.28s   "How it works" section — 3 steps:
                                                Fund → Arbiter → Settle
 4  connect        38.88s    46.24s    7.36s   Launch Demo click → demo.html loads →
                                                "Offline Demo" connect screen
 5  create         46.24s    61.84s   15.60s   Workspace (3-column): contract addresses,
                                                balances, escrow form → fill → create
 6  dispute        61.84s    67.52s    5.68s   Dispute button click → escrow freezes →
                                                pipeline activates
 7  pipeline       67.52s    84.80s   17.28s   4-agent pipeline visualization:
                                                Verifier → Arbiter → Policy → KeeperHub
 8  arbiter        84.80s   103.60s   18.80s   Terminal: Groq LLM arbiter-runner.ts
                                                output (2 scenarios + verdicts)
 9  blockscout    103.60s   115.44s   11.84s   Blockscout Sepolia TX page:
                                                resolveDispute, 9.9 USDC refund, Success
10  closing       115.44s   120.16s    4.72s   Closing card: "The agent decides.
                                                KeeperHub executes." + recourse.dev
─── ────────────── ────────  ────────  ──────── ─────────────────────────────────────────

SCENE TRANSITIONS
──────────────────────────────────────────────────────────────────────
  13.76s   hero → primitives     (landing hero → "What it does" cards)
  27.60s   primitives → how      ("What it does" → "How it works" steps)
  38.88s   how → connect         ("How it works" → Launch Demo click)
  46.24s   connect → create      (connect screen → workspace + escrow form)
  61.84s   create → dispute      (escrow created → dispute raised)
  67.52s   dispute → pipeline    (dispute active → 4-agent pipeline)
  84.80s   pipeline → arbiter    (pipeline viz → terminal arbiter output)
 103.60s   arbiter → blockscout  (terminal → Blockscout transaction page)
 115.44s   blockscout → closing  (Blockscout TX → closing card)

══════════════════════════════════════════════════════════════════════
SYNC ISSUES — DETECTED IN RECORDING
══════════════════════════════════════════════════════════════════════

1. [connect] SCROLL-BACK DELAY — 3-4s desync
   The narration at 38.88s says "Clicking Launch Demo..." but the
   recording must first smoothScroll from #howitworks back to hero
   top (~2-3s), then move+click the button (~0.5-1s), then wait for
   demo.html to load (~1-2s). The actual visual click happens at ~42s,
   3s after the narration mentions it.

2. [connect] CONNECT SCREEN TIMING — tight
   demo.html connect screen appears at ~44s. "Offline Demo" is clicked
   at ~44.5s. Workspace loads at ~46s — right when scene 5 narration
   begins. No time for the viewer to register the connect screen.

3. [create] FORM FILL SPEED — OK
   Form fill via JS evaluate() is fast (~0.6s). Create escrow click
   + TX confirmation takes ~8s. Workspace visible by ~60s, narration
   ends at 61.84s. Roughly aligned.

4. [pipeline] VISUAL PACING — depends on animation
   Recording just sleeps 17.3s after dispute click. The pipeline UI
   animation from demo.html must fill the entire duration. If the
   animation is shorter, there's dead screen time.

5. [blockscout] LOADING LATENCY — 5-8s of blank/loading
   Real Blockscout URL loaded with 7s timeout. At 110s, page is still
   loading. Full TX details appear at ~115s, only 0.4s before narration
   ends. If network is slow, falls back to static HTML card.

══════════════════════════════════════════════════════════════════════
RECORDING SCRIPT ANALYSIS (record-v5.cjs)
══════════════════════════════════════════════════════════════════════

Scene 4 (line 87): smoothScroll(page, 0, 100, 200) scrolls from
  #howitworks back to hero. With stepPx=200 and stepMs=100, a
  ~1500px scroll takes ~7-8 steps = ~700-800ms + 200ms final sleep.

Scene 4 (line 89): moveAndClick('a.btn.btn-primary[href="demo.html"]')
  has mouse movement (6+18 steps) + delay:60ms + after:1500ms.
  Total: ~2s from scroll-complete to click complete.

Scene 5 (line 96-98): Waits for connectOfflineDemo button, then clicks.
  After: 1000ms. Then waits for workspace to appear (waitForFunction).

Scene 9 (line 197): Real Blockscout URL with 7s timeout. Falls back
  to static HTML card if content check fails.

Scene 10 (line 211): setContent to closing card, sleeps 4700ms.
  This is the shortest scene — only 4.72s.

══════════════════════════════════════════════════════════════════════
IDEAL FIX (scene 4)
══════════════════════════════════════════════════════════════════════

To fix the 3-4s desync in scene 4, the recording script should:

Option A: Pre-scroll back to hero during the tail of scene 3
  → Start smoothScroll to top at ~36s (2-3s before narration transition)
  → By 38.88s, hero/launch button is already visible
  → Click Launch Demo immediately at 38.88s

Option B: Extend scene 3 to absorb the scroll-back
  → Give scene 3 the full 13.8s (including scroll-back time)
  → Start scene 4 narration only after the click happens
  → Requires re-ordering: narration clips 3+4 would need re-recording

Option C: Cut the scroll-back and hard-cut to demo.html
  → Use setContent or page.goto to jump directly to demo.html at 38.88s
  → Skip the visual scroll-back entirely
  → Most surgical fix, loses the "click" visual but gains sync
